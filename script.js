const menu=document.querySelector('.hamburger'),nav=document.querySelector('.nav');if(menu&&nav){menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const filters=document.querySelectorAll('.filters button'),posts=document.querySelectorAll('.post');filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('selected'));btn.classList.add('selected');const f=btn.dataset.filter;posts.forEach(p=>p.style.display=f==='all'||p.dataset.cat===f?'':'none')}));
